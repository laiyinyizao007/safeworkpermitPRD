# 4. 系统架构设计

## 4.1 "1+8"架构模式概述

### 架构理念

危险化学品企业特殊作业许可(PTW)管理系统采用**"1+8"架构模式**:
- **"1"**: 通用底座(Universal Base Platform) - 提供共享能力和基础设施
- **"8"**: 8个作业票模块(Operation Modules) - 独立开发、独立部署、独立升级

**核心优势**:
- ✅ **代码复用**: 通用能力统一管理,避免重复开发
- ✅ **数据共享**: 气体分析、人员资质、监护记录一次录入多处调用
- ✅ **灵活配置**: 企业可根据实际需求选择上线的作业模块
- ✅ **独立演进**: 各模块独立迭代,互不影响
- ✅ **易于扩展**: 新增作业类型只需开发新模块,无需改动底座

### 架构全景图

```mermaid
flowchart TB
    subgraph 用户层
        A1[作业申请人<br/>移动端/PC端]
        A2[现场监护人<br/>移动端]
        A3[审批人<br/>移动端/PC端]
        A4[安全管理员<br/>PC端]
        A5[系统管理员<br/>PC端]
    end

    subgraph 应用层
        B1[动火作业模块]
        B2[受限空间作业模块]
        B3[盲板抽堵作业模块]
        B4[高处作业模块]
        B5[吊装作业模块]
        B6[临时用电作业模块]
        B7[动土作业模块]
        B8[断路作业模块]
    end

    subgraph 通用底座
        C1[基础信息管理]
        C2[人员库]
        C3[JSA风险库]
        C4[IoT接口层]
        C5[定位引擎]
        C6[审批引擎]
        C7[数据共享层]
    end

    subgraph 数据层
        D1[(主数据库<br/>PostgreSQL)]
        D2[(缓存<br/>Redis)]
        D3[(文件存储<br/>MinIO)]
        D4[(时序数据库<br/>InfluxDB)]
    end

    subgraph 外部系统
        E1[气体检测仪]
        E2[定位设备]
        E3[视频监控]
        E4[环境传感器]
        E5[CA认证中心]
    end

    A1 & A2 & A3 & A4 & A5 --> B1 & B2 & B3 & B4 & B5 & B6 & B7 & B8
    B1 & B2 & B3 & B4 & B5 & B6 & B7 & B8 --> C1 & C2 & C3 & C4 & C5 & C6 & C7
    C1 & C2 & C3 & C6 & C7 --> D1
    C4 --> D2
    C5 --> D2
    C7 --> D3
    C4 --> D4
    C4 <--> E1 & E2 & E3 & E4
    C6 <--> E5
```

---

## 4.2 通用底座(Universal Base Platform)

### 4.2.1 基础信息管理

**职责**:
- 企业组织架构管理
- 区域划分管理
- 设备台账管理
- 系统配置管理

**核心功能**:
- 组织架构树(支持多级部门)
- 区域地理信息(经纬度、边界、层级)
- 设备分类与编码
- 系统参数配置(审批流程、时效规则等)

**数据模型**:
```mermaid
erDiagram
    ORGANIZATION ||--o{ DEPARTMENT : contains
    DEPARTMENT ||--o{ USER : belongs_to
    ORGANIZATION ||--o{ AREA : contains
    AREA ||--o{ EQUIPMENT : located_in
    EQUIPMENT ||--o{ WORK_PERMIT : involves

    ORGANIZATION {
        string org_id PK
        string org_name
        string org_code
        string legal_person
        datetime created_at
    }

    DEPARTMENT {
        string dept_id PK
        string org_id FK
        string dept_name
        string parent_dept_id
        int level
    }

    AREA {
        string area_id PK
        string org_id FK
        string area_name
        string area_type
        float latitude
        float longitude
        json boundary
    }

    EQUIPMENT {
        string equipment_id PK
        string area_id FK
        string equipment_name
        string equipment_type
        string equipment_code
        string status
    }
```

### 4.2.2 人员库

**职责**:
- 人员基本信息管理
- 资质证书管理
- 培训记录管理
- 健康档案管理

**核心功能**:
- 人员信息CRUD
- 资质证书有效期管理(自动提醒到期)
- 培训记录关联(与作业类型匹配)
- 健康状态检查(体检报告、职业禁忌症)
- 人脸特征向量存储(用于生物识别)

**数据模型**:
```mermaid
erDiagram
    USER ||--o{ CERTIFICATE : has
    USER ||--o{ TRAINING_RECORD : has
    USER ||--o{ HEALTH_RECORD : has
    USER ||--o{ FACE_FEATURE : has

    USER {
        string user_id PK
        string user_name
        string id_card
        string dept_id FK
        string phone
        string email
        string status
    }

    CERTIFICATE {
        string cert_id PK
        string user_id FK
        string cert_type
        string cert_number
        date issue_date
        date expire_date
        string issuer
        string status
    }

    TRAINING_RECORD {
        string training_id PK
        string user_id FK
        string training_type
        date training_date
        int duration_hours
        string trainer
        string result
    }

    HEALTH_RECORD {
        string health_id PK
        string user_id FK
        date exam_date
        string exam_result
        json contraindications
        date next_exam_date
    }

    FACE_FEATURE {
        string feature_id PK
        string user_id FK
        binary feature_vector
        datetime created_at
        datetime updated_at
    }
```

### 4.2.3 JSA风险库

**职责**:
- 标准风险库管理(基于GB 30871-2022)
- 企业自定义风险库管理
- 风险与作业类型关联
- 安全措施库管理

**核心功能**:
- 8类作业的标准风险库(预置)
- 企业自定义风险添加
- 风险等级评估(L×E×C模型)
- 安全措施推荐引擎
- 风险库版本管理

**数据模型**:
```mermaid
erDiagram
    RISK_CATEGORY ||--o{ RISK_ITEM : contains
    RISK_ITEM ||--o{ SAFETY_MEASURE : requires
    WORK_PERMIT ||--o{ RISK_ASSESSMENT : has
    RISK_ASSESSMENT }o--|| RISK_ITEM : references

    RISK_CATEGORY {
        string category_id PK
        string category_name
        string work_type
        string description
    }

    RISK_ITEM {
        string risk_id PK
        string category_id FK
        string risk_name
        string risk_description
        int likelihood
        int exposure
        int consequence
        int risk_level
        string source
    }

    SAFETY_MEASURE {
        string measure_id PK
        string risk_id FK
        string measure_name
        string measure_description
        string measure_type
        boolean is_mandatory
    }

    RISK_ASSESSMENT {
        string assessment_id PK
        string permit_id FK
        string risk_id FK
        string assessment_result
        json selected_measures
        datetime assessed_at
    }
```

### 4.2.4 IoT接口层

**职责**:
- 气体检测仪集成
- 定位设备集成
- 视频监控集成
- 环境传感器集成

**核心功能**:
- 设备注册与认证
- 数据采集与解析(支持多种协议: MQTT, HTTP, WebSocket)
- 数据清洗与校验
- 异常数据告警
- 设备状态监控

**技术架构**:
```mermaid
flowchart LR
    subgraph IoT设备
        A1[气体检测仪<br/>MQTT]
        A2[定位设备<br/>HTTP]
        A3[视频监控<br/>RTSP]
        A4[环境传感器<br/>WebSocket]
    end

    subgraph IoT接口层
        B1[设备接入网关]
        B2[协议适配器]
        B3[数据解析器]
        B4[数据清洗器]
        B5[数据校验器]
    end

    subgraph 数据存储
        C1[时序数据库<br/>InfluxDB]
        C2[缓存<br/>Redis]
    end

    subgraph 应用层
        D1[实时监控]
        D2[告警引擎]
        D3[数据分析]
    end

    A1 & A2 & A3 & A4 --> B1
    B1 --> B2
    B2 --> B3
    B3 --> B4
    B4 --> B5
    B5 --> C1 & C2
    C1 & C2 --> D1 & D2 & D3
```

### 4.2.5 定位引擎

**职责**:
- 地理围栏管理
- 空间冲突检测
- 动态隔离带管理
- 人员定位追踪

**核心功能**:
- 地理围栏创建与管理(支持多边形、圆形)
- 实时位置监控(基于UWB或北斗)
- 空间冲突检测算法(基于GIS)
- 动态隔离带计算(如动火点周围15米)
- 人员进出围栏告警

**空间冲突检测算法**:
```mermaid
flowchart TD
    A[新作业申请] --> B{获取作业位置}
    B --> C[查询进行中的作业]
    C --> D{遍历每个作业}
    D --> E{计算距离}
    E --> F{距离 < 隔离距离?}
    F -->|是| G{作业类型冲突?}
    F -->|否| D
    G -->|是| H[标记为冲突]
    G -->|否| D
    D --> I{遍历完成?}
    I -->|否| D
    I -->|是| J{存在冲突?}
    J -->|是| K[阻断审批<br/>提示冲突信息]
    J -->|否| L[允许继续审批]
```

**冲突规则示例**:
| 作业类型A | 作业类型B | 最小隔离距离 | 冲突原因 |
|----------|----------|------------|---------|
| 动火作业 | 受限空间作业 | 15米 | 火花可能引燃可燃气体 |
| 动火作业 | 动火作业 | 30米 | 火源叠加风险 |
| 动火作业 | 喷漆作业 | 50米 | 可燃蒸汽扩散 |
| 吊装作业 | 高处作业 | 20米 | 物体打击风险 |

### 4.2.6 审批引擎

**职责**:
- 工作流引擎
- 权限管理
- 电子签名
- CA认证集成

**核心功能**:
- 动态审批流配置(基于作业等级)
- 条件分支审批(如特级动火需总工程师审批)
- 审批权限漂移(审批人不在岗时自动上浮)
- 电子签名集成(调用CA数字证书)
- 审批超时提醒
- 审批记录全程留痕

**审批流程示例(动火作业)**:
```mermaid
flowchart TD
    A[作业申请] --> B{作业等级?}
    B -->|特级动火| C[安全员审批]
    B -->|一级动火| D[安全员审批]
    B -->|二级动火| E[安全员审批]
    C --> F[车间主任审批]
    D --> G[车间主任审批]
    E --> H{审批通过?}
    F --> I[总工程师审批]
    G --> J{审批通过?}
    I --> K{审批通过?}
    J -->|是| L[票证生效]
    J -->|否| M[驳回]
    K -->|是| L
    K -->|否| M
    H -->|是| L
    H -->|否| M
```

### 4.2.7 数据共享层

**职责**:
- 气体分析数据共享
- 监护记录共享
- 安全措施共享
- 应急预案共享

**核心功能**:
- 数据订阅与发布(基于事件驱动)
- 数据版本管理
- 数据权限控制
- 数据同步状态监控

**数据共享流程**:
```mermaid
sequenceDiagram
    autonumber
    participant A as 受限空间作业票
    participant B as 数据共享层
    participant C as 动火作业票
    participant D as 气体检测仪

    D->>B: 上传气体分析数据
    B->>B: 存储数据
    A->>B: 订阅气体分析数据
    B->>A: 推送数据
    C->>B: 查询气体分析数据
    B->>C: 返回数据
    Note over A,C: 一次分析,多处调用
```

---

## 4.3 8大作业票模块

### 4.3.1 模块通用结构

每个作业票模块包含以下标准组件:

```mermaid
flowchart TB
    subgraph 作业票模块
        A[作业申请表单]
        B[作业分级逻辑]
        C[审批流程配置]
        D[安全措施清单]
        E[现场监护要求]
        F[完工验收标准]
    end

    subgraph 通用底座
        G[人员库]
        H[JSA风险库]
        I[审批引擎]
        J[数据共享层]
    end

    A --> B
    B --> C
    C --> I
    A --> D
    D --> H
    A --> E
    E --> G
    F --> J
```

### 4.3.2 模块列表与核心差异

| 模块名称 | 作业分级 | 特殊要求 | 关键风险点 |
|---------|---------|---------|-----------|
| **动火作业** | 特级/一级/二级 | 气体分析(可燃气体、氧含量)<br/>动态隔离带(15米/30米) | 火灾、爆炸 |
| **受限空间作业** | 一级/二级 | 气体分析(氧含量、有毒气体、可燃气体)<br/>通风措施<br/>应急救援预案 | 中毒、窒息、爆炸 |
| **盲板抽堵作业** | 一级/二级 | 压力泄放<br/>介质确认<br/>盲板编号管理 | 泄漏、中毒、灼伤 |
| **高处作业** | 一级/二级/三级/四级 | 高度分级(2-5米/5-15米/15-30米/>30米)<br/>防坠落措施<br/>安全带检查 | 坠落、物体打击 |
| **吊装作业** | 一级/二级 | 吊装重量<br/>吊装高度<br/>吊装方案 | 物体打击、起重伤害 |
| **临时用电作业** | 一级/二级 | 电源隔离<br/>接地保护<br/>漏电保护 | 触电、火灾 |
| **动土作业** | 一级/二级 | 地下管线探测<br/>开挖深度<br/>支护方案 | 管线损坏、中毒、窒息 |
| **断路作业** | 一级/二级 | 交通管制<br/>警示标识<br/>应急通道 | 交通事故、作业冲突 |

### 4.3.3 模块间交叉作业关联

**交叉作业自动识别规则**:
```mermaid
flowchart TD
    A[新作业申请] --> B{作业位置分析}
    B --> C{是否在受限空间内?}
    C -->|是| D[强制关联受限空间作业票]
    C -->|否| E{作业高度 ≥ 2米?}
    E -->|是| F[强制关联高处作业票]
    E -->|否| G{是否涉及吊装?}
    G -->|是| H[强制关联吊装作业票]
    G -->|否| I{是否涉及临时用电?}
    I -->|是| J[强制关联临时用电作业票]
    I -->|否| K[单一作业]
    D --> L[生成交叉作业关联记录]
    F --> L
    H --> L
    J --> L
    L --> M[共享数据<br/>气体分析/监护记录]
```

---

## 4.4 技术架构

### 4.4.1 总体技术架构

```mermaid
flowchart TB
    subgraph 前端层
        A1[移动端<br/>React Native]
        A2[PC端<br/>React + Ant Design]
        A3[管理后台<br/>Vue + Element UI]
    end

    subgraph API网关层
        B1[API Gateway<br/>Kong]
        B2[认证中心<br/>OAuth 2.0 + JWT]
        B3[限流熔断<br/>Sentinel]
    end

    subgraph 应用服务层
        C1[作业票服务<br/>Spring Boot]
        C2[审批服务<br/>Spring Boot]
        C3[IoT服务<br/>Spring Boot]
        C4[定位服务<br/>Spring Boot]
        C5[通知服务<br/>Spring Boot]
    end

    subgraph 数据层
        D1[(主数据库<br/>PostgreSQL)]
        D2[(缓存<br/>Redis)]
        D3[(文件存储<br/>MinIO)]
        D4[(时序数据库<br/>InfluxDB)]
        D5[(搜索引擎<br/>Elasticsearch)]
    end

    subgraph 基础设施层
        E1[消息队列<br/>RabbitMQ]
        E2[任务调度<br/>XXL-Job]
        E3[日志中心<br/>ELK]
        E4[监控中心<br/>Prometheus + Grafana]
    end

    A1 & A2 & A3 --> B1
    B1 --> B2 & B3
    B2 & B3 --> C1 & C2 & C3 & C4 & C5
    C1 & C2 & C3 & C4 & C5 --> D1 & D2 & D3 & D4 & D5
    C1 & C2 & C3 & C4 & C5 --> E1 & E2
    E1 & E2 --> E3 & E4
```

### 4.4.2 核心技术栈选型

| 技术分类 | 技术选型 | 选型理由 |
|---------|---------|---------|
| **前端框架** | React Native (移动端)<br/>React (PC端) | 跨平台、生态成熟、性能优秀 |
| **后端框架** | Spring Boot | 企业级、生态完善、易于维护 |
| **API网关** | Kong | 高性能、插件丰富、易于扩展 |
| **主数据库** | PostgreSQL | 开源、支持GIS扩展(PostGIS)、事务性强 |
| **缓存** | Redis | 高性能、支持多种数据结构、持久化 |
| **文件存储** | MinIO | 开源、S3兼容、易于部署 |
| **时序数据库** | InfluxDB | 专为时序数据设计、高性能写入 |
| **搜索引擎** | Elasticsearch | 全文检索、日志分析、实时搜索 |
| **消息队列** | RabbitMQ | 可靠性高、支持多种消息模式 |
| **任务调度** | XXL-Job | 分布式、易于管理、支持动态调度 |
| **监控** | Prometheus + Grafana | 开源、生态完善、可视化强 |

### 4.4.3 部署架构

```mermaid
flowchart TB
    subgraph 用户终端
        A1[移动端APP]
        A2[PC浏览器]
    end

    subgraph DMZ区
        B1[负载均衡<br/>Nginx]
        B2[API网关<br/>Kong]
    end

    subgraph 应用区
        C1[应用服务集群<br/>Kubernetes]
        C2[缓存集群<br/>Redis Cluster]
    end

    subgraph 数据区
        D1[数据库主从<br/>PostgreSQL]
        D2[文件存储<br/>MinIO Cluster]
        D3[时序数据库<br/>InfluxDB]
    end

    subgraph 监控区
        E1[监控中心<br/>Prometheus]
        E2[日志中心<br/>ELK]
    end

    A1 & A2 --> B1
    B1 --> B2
    B2 --> C1
    C1 --> C2
    C1 --> D1 & D2 & D3
    C1 --> E1 & E2
```

---

**本章节完成时间**: 2026-03-09
**文档维护者**: Claude Code (Opus 4.6)
